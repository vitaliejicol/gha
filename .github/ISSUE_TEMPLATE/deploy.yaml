name: "Deploy Request"
description: "Trigger deployment to UAT or PROD"
title: "Deploy to {{ environment }}"
body:
  - type: dropdown
    id: environment
    attributes:
      label: Environment
      options:
        - uat
        - prod
    validations:
      required: true
